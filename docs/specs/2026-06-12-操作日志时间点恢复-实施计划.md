# 操作日志驱动的时间点恢复 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:subagent-driven-development 或 superpowers:executing-plans 逐任务实施。步骤用 `- [ ]` 勾选跟踪。

**Goal:** 给宿舍管理系统加一张"操作日志表"记录所有数据改动，并在备份恢复窗体里做成可点选的时间线——点中某条操作即可整库回滚到那个时刻，替代手动记/猜时间。

**Architecture:** 复用既有"完整+日志备份 + STOPAT 恢复"引擎。新增 `OperationLog` 表；写操作经 `DBHelper.ExecuteLogged` 在同一事务内写数据 + 记日志；备份窗体加时间线 grid，选中行→计算 STOPAT→调用现有恢复。

**Tech Stack:** SQL Server（T-SQL）、C# WinForms（net10.0-windows）、Microsoft.Data.SqlClient。

> **验证方式：** 本项目为 WinForms + 实库，沿用既有两份实施计划的做法——每个任务 `dotnet build` 通过 + 手动运行测试，而非自动化单测。`BUILD` 命令统一为：
> `dotnet build "D:\DataBase\KESHE\src\DormManagement\DormManagement.csproj" -v q -nologo`

---

## 文件结构

| 文件 | 职责 | 改动 |
|---|---|---|
| `sql/10_operation_log.sql` | OperationLog 表 | 新增 |
| `src/DormManagement/Session.cs` | 运行期当前操作员 | 新增 |
| `src/DormManagement/DAL/DBHelper.cs` | 加 `ExecuteLogged` / `Log` | 修改 |
| `src/DormManagement/Forms/MainForm.cs` | 登录后设置 `Session.CurrentOperator` | 修改 |
| `Forms/BuildingForm.cs` `ClassForm.cs` `StudentForm.cs` `RoomForm.cs` | 增删改改用 `ExecuteLogged`/`Log` | 修改 |
| `Forms/CheckInForm.cs` `TransferForm.cs` `CheckOutForm.cs` | 写分配改用 `ExecuteLogged`（HousingLog 保留） | 修改 |
| `Forms/BackupRestoreForm.cs` | 加操作时间线 + 之前/之后按钮 | 修改 |
| `docs/用户使用手册.md` | 更新备份章节 | 修改 |

---

## Task 1：数据库 — OperationLog 表

**Files:** Create `sql/10_operation_log.sql`

- [ ] **Step 1: 写脚本**

```sql
-- 10_operation_log.sql  通用操作日志（供时间点恢复的可点选时间线使用）
USE DormDB;
GO
IF OBJECT_ID('OperationLog','U') IS NOT NULL DROP TABLE OperationLog;
GO
CREATE TABLE OperationLog (
    op_id       INT IDENTITY(1,1) PRIMARY KEY,
    op_time     DATETIME2(3) NOT NULL DEFAULT SYSDATETIME(),  -- 毫秒精度，供 STOPAT 使用
    category    NVARCHAR(20) NOT NULL,   -- 宿舍楼/班级/学生/寝室/入住/调宿/退宿
    action      NVARCHAR(10) NOT NULL,   -- 新增/修改/删除
    description NVARCHAR(200) NULL,
    operator    NVARCHAR(50)  NULL
);
GO
```

- [ ] **Step 2: 执行** — SSMS F5 或 `sqlcmd -S localhost -E -C -i "sql\10_operation_log.sql"`。

- [ ] **Step 3: 验证**

```sql
INSERT INTO OperationLog(category,action,description,operator) VALUES('测试','新增','冒烟','tester');
SELECT TOP 1 op_id, op_time, category, action, description, operator FROM OperationLog ORDER BY op_id DESC;
DELETE FROM OperationLog WHERE category='测试';
```
预期：能插入、op_time 自动有毫秒时间、能查到再删掉。

- [ ] **Step 4: 提交**

```bash
git add sql/10_operation_log.sql
git commit -m "db: 新增 OperationLog 操作日志表"
```

---

## Task 2：Session 静态类 + 登录设置操作员

**Files:** Create `src/DormManagement/Session.cs`；Modify `src/DormManagement/Forms/MainForm.cs`

- [ ] **Step 1: 新建 `Session.cs`**

```csharp
namespace DormManagement
{
    /// <summary>运行期会话状态：保存当前登录操作员，供操作日志记录使用</summary>
    public static class Session
    {
        public static string CurrentOperator { get; set; } = "";
    }
}
```

- [ ] **Step 2: 在 `MainForm` 构造函数开头设置操作员**

`MainForm.cs` 构造函数现有第一行是 `_operator = loginName;`，在其后加一行：

```csharp
            _operator = loginName;
            Session.CurrentOperator = loginName;   // 供操作日志记录使用
```

- [ ] **Step 3: 构建** — 运行 `BUILD`，预期 0 错误。

- [ ] **Step 4: 提交**

```bash
git add src/DormManagement/Session.cs src/DormManagement/Forms/MainForm.cs
git commit -m "feat: 加 Session.CurrentOperator，登录后记录当前操作员"
```

---

## Task 3：DBHelper 增加 ExecuteLogged / Log

**Files:** Modify `src/DormManagement/DAL/DBHelper.cs`

- [ ] **Step 1: 在 DBHelper 类内、`ProcTable` 方法之后加两个方法**

```csharp
        // 在单个事务内执行数据写入 + 记一条操作日志，保证两者一致、时间戳同步
        public static int ExecuteLogged(string dataSql, SqlParameter[] dataParams,
            string category, string action, string description)
        {
            using var conn = new SqlConnection(AppConfig.ConnectionString);
            conn.Open();
            using var tran = conn.BeginTransaction();
            try
            {
                int n;
                using (var cmd = new SqlCommand(dataSql, conn, tran))
                {
                    if (dataParams != null) cmd.Parameters.AddRange(dataParams);
                    n = cmd.ExecuteNonQuery();
                }
                using (var log = new SqlCommand(
                    "INSERT INTO OperationLog(category,action,description,operator) VALUES(@c,@a,@d,@o)",
                    conn, tran))
                {
                    log.Parameters.AddRange(new[] {
                        new SqlParameter("@c", category),
                        new SqlParameter("@a", action),
                        new SqlParameter("@d", (object?)description ?? DBNull.Value),
                        new SqlParameter("@o", string.IsNullOrWhiteSpace(Session.CurrentOperator)
                            ? (object)DBNull.Value : Session.CurrentOperator) });
                    log.ExecuteNonQuery();
                }
                tran.Commit();
                return n;
            }
            catch { tran.Rollback(); throw; }
        }

        // 仅记一条操作日志（用于多语句操作如"新增寝室含建床位"，成功后best-effort调用）
        public static void Log(string category, string action, string description)
            => Execute("INSERT INTO OperationLog(category,action,description,operator) VALUES(@c,@a,@d,@o)",
                new SqlParameter("@c", category),
                new SqlParameter("@a", action),
                new SqlParameter("@d", (object?)description ?? DBNull.Value),
                new SqlParameter("@o", string.IsNullOrWhiteSpace(Session.CurrentOperator)
                    ? (object)DBNull.Value : Session.CurrentOperator));
```

> `DBHelper` 命名空间是 `DormManagement.DAL`，`Session` 在 `DormManagement`，外层命名空间可直接引用（与现有 `AppConfig` 用法一致），无需额外 using。

- [ ] **Step 2: 构建** — `BUILD`，预期 0 错误。

- [ ] **Step 3: 提交**

```bash
git add src/DormManagement/DAL/DBHelper.cs
git commit -m "feat(dal): ExecuteLogged 事务内写数据+记操作日志，Log 单记日志"
```

---

## Task 4：宿舍楼 / 班级窗体记录日志

**Files:** Modify `Forms/BuildingForm.cs`、`Forms/ClassForm.cs`

- [ ] **Step 1: BuildingForm — 改 `Add` 里的写入语句**

把 `Add()` 中的 `DBHelper.Execute(...)` 整段替换为：

```csharp
                DBHelper.ExecuteLogged(
                    "INSERT INTO Building(building_no,building_name,gender_type) VALUES(@n,@m,@g)",
                    new[] {
                        new SqlParameter("@n", txtNo.Text.Trim()),
                        new SqlParameter("@m", NullIfEmpty(txtName.Text)),
                        new SqlParameter("@g", NullIfEmpty(cmbGender.Text)) },
                    "宿舍楼", "新增", $"新增宿舍楼 {txtNo.Text.Trim()}");
```

- [ ] **Step 2: BuildingForm — 改 `Upd`**

把 `Upd()` 中的 `DBHelper.Execute(...)` 替换为：

```csharp
            DBHelper.ExecuteLogged(
                "UPDATE Building SET building_no=@n,building_name=@m,gender_type=@g WHERE building_id=@id",
                new[] {
                    new SqlParameter("@n", txtNo.Text.Trim()),
                    new SqlParameter("@m", NullIfEmpty(txtName.Text)),
                    new SqlParameter("@g", NullIfEmpty(cmbGender.Text)),
                    new SqlParameter("@id", id) },
                "宿舍楼", "修改", $"修改宿舍楼 {txtNo.Text.Trim()}（编号{id}）");
```

- [ ] **Step 3: BuildingForm — 改 `Del`**

把 `Del()` 中 `if (MessageBox...)` 之后的 try 块改为（先取楼号再删）：

```csharp
            var no = grid.CurrentRow?.Cells["楼号"].Value?.ToString() ?? id.ToString();
            try
            {
                DBHelper.ExecuteLogged("DELETE FROM Building WHERE building_id=@id",
                    new[] { new SqlParameter("@id", id) },
                    "宿舍楼", "删除", $"删除宿舍楼 {no}");
                LoadData();
            }
            catch (SqlException ex) when (ex.Number == 547) { MessageBox.Show("该楼下还有寝室，无法删除"); }
```

- [ ] **Step 4: ClassForm — 改 `Add`**

```csharp
                DBHelper.ExecuteLogged("INSERT INTO ClassInfo(class_name,major) VALUES(@n,@m)",
                    new[] {
                        new SqlParameter("@n", txtName.Text.Trim()),
                        new SqlParameter("@m", txtMajor.Text.Trim()) },
                    "班级", "新增", $"新增班级 {txtName.Text.Trim()}");
```

- [ ] **Step 5: ClassForm — 改 `Upd`**

```csharp
            DBHelper.ExecuteLogged("UPDATE ClassInfo SET class_name=@n,major=@m WHERE class_id=@id",
                new[] {
                    new SqlParameter("@n", txtName.Text.Trim()),
                    new SqlParameter("@m", txtMajor.Text.Trim()),
                    new SqlParameter("@id", id) },
                "班级", "修改", $"修改班级 {txtName.Text.Trim()}（编号{id}）");
```

- [ ] **Step 6: ClassForm — 改 `Del`**

```csharp
            var cname = grid.CurrentRow?.Cells["班级名"].Value?.ToString() ?? id.ToString();
            try
            {
                DBHelper.ExecuteLogged("DELETE FROM ClassInfo WHERE class_id=@id",
                    new[] { new SqlParameter("@id", id) },
                    "班级", "删除", $"删除班级 {cname}");
                LoadData();
            }
            catch (SqlException ex) when (ex.Number == 547) { MessageBox.Show("该班级下还有学生，无法删除"); }
```

- [ ] **Step 7: 构建** — `BUILD`，预期 0 错误。

- [ ] **Step 8: 手测** — `dotnet run`，登录后在"宿舍楼管理"新增一栋楼；SSMS 执行 `SELECT TOP 5 * FROM OperationLog ORDER BY op_id DESC;`，预期出现一条 `宿舍楼/新增/新增宿舍楼 .../操作人=admin`。班级同理。

- [ ] **Step 9: 提交**

```bash
git add src/DormManagement/Forms/BuildingForm.cs src/DormManagement/Forms/ClassForm.cs
git commit -m "feat: 宿舍楼/班级增删改记录操作日志"
```

---

## Task 5：学生 / 寝室窗体记录日志

**Files:** Modify `Forms/StudentForm.cs`、`Forms/RoomForm.cs`

- [ ] **Step 1: StudentForm — 改 `Add`**

把 `Add()` 里 `DBHelper.Execute("INSERT INTO Student...")` 替换为：

```csharp
                DBHelper.ExecuteLogged(
                    "INSERT INTO Student(student_no,name,gender,class_id,phone) VALUES(@no,@nm,@g,@c,@p)",
                    new[] {
                        new SqlParameter("@no", txtNo.Text.Trim()),
                        new SqlParameter("@nm", txtName.Text.Trim()),
                        new SqlParameter("@g", NullIfEmpty(cmbGender.Text)),
                        new SqlParameter("@c", ClassIdParam()),
                        new SqlParameter("@p", NullIfEmpty(txtPhone.Text)) },
                    "学生", "新增", $"新增学生 {txtName.Text.Trim()}({txtNo.Text.Trim()})");
```

- [ ] **Step 2: StudentForm — 改 `Upd`**

```csharp
            DBHelper.ExecuteLogged(
                "UPDATE Student SET student_no=@no,name=@nm,gender=@g,class_id=@c,phone=@p WHERE student_id=@id",
                new[] {
                    new SqlParameter("@no", txtNo.Text.Trim()),
                    new SqlParameter("@nm", txtName.Text.Trim()),
                    new SqlParameter("@g", NullIfEmpty(cmbGender.Text)),
                    new SqlParameter("@c", ClassIdParam()),
                    new SqlParameter("@p", NullIfEmpty(txtPhone.Text)),
                    new SqlParameter("@id", id) },
                "学生", "修改", $"修改学生 {txtName.Text.Trim()}({txtNo.Text.Trim()})");
```

- [ ] **Step 3: StudentForm — 改 `Del`**

把 `Del()` 的 try 块改为（先取姓名/学号）：

```csharp
            var sno = grid.CurrentRow?.Cells["学号"].Value?.ToString() ?? "";
            var sname = grid.CurrentRow?.Cells["姓名"].Value?.ToString() ?? "";
            try
            {
                DBHelper.ExecuteLogged("DELETE FROM Student WHERE student_id=@id",
                    new[] { new SqlParameter("@id", id) },
                    "学生", "删除", $"删除学生 {sname}({sno})");
                LoadData();
            }
            catch (SqlException ex) when (ex.Number == 547) { MessageBox.Show("该学生有住宿/记录，需先退宿后再删除"); }
```

- [ ] **Step 4: RoomForm — `Add` 末尾加日志（多语句，用 Log）**

在 `Add()` 的 `for` 床位循环之后、`LoadData();` 之前插入一行：

```csharp
                DBHelper.Log("寝室", "新增", $"新增寝室 房号{txtRoomNo.Text.Trim()}（{cap}床）");
                LoadData();
```

- [ ] **Step 5: RoomForm — 改 `Rename`**

把 `Rename()` 里 `DBHelper.Execute("UPDATE Room SET room_no...")` 替换为：

```csharp
                DBHelper.ExecuteLogged("UPDATE Room SET room_no=@r WHERE room_id=@id",
                    new[] {
                        new SqlParameter("@r", txtRoomNo.Text.Trim()),
                        new SqlParameter("@id", id) },
                    "寝室", "修改", $"寝室改房号 → {txtRoomNo.Text.Trim()}（编号{id}）");
```

- [ ] **Step 6: RoomForm — 改 `Del`**

把 `Del()` 里事务删除那段 `DBHelper.Execute(@"BEGIN TRY ...")` 替换为（先取房号；**不要**再写内层 BEGIN TRAN/ROLLBACK——`ExecuteLogged` 已提供客户端事务，把两条 DELETE + 日志一并原子提交）：

```csharp
            var rno = grid.CurrentRow?.Cells["房号"].Value?.ToString() ?? id.ToString();
            try
            {
                DBHelper.ExecuteLogged(
                    @"DELETE FROM Bed WHERE room_id=@id;
                      DELETE FROM Room WHERE room_id=@id;",
                    new[] { new SqlParameter("@id", id) },
                    "寝室", "删除", $"删除寝室 房号{rno}");
                LoadData();
            }
            catch (SqlException ex) when (ex.Number == 547) { MessageBox.Show("寝室内有住宿记录，无法删除"); }
```

> 注：删床+删寝室+记日志都在 `ExecuteLogged` 的同一事务内；若删寝室因外键失败(547)，整个事务（含已删的床位）回滚，并把 547 抛回给上面的 `catch`，行为与原来一致且更简洁。**切勿**在 dataSql 里再写 `BEGIN TRAN/ROLLBACK`，否则会与客户端事务冲突。

- [ ] **Step 7: 构建** — `BUILD`，预期 0 错误。

- [ ] **Step 8: 手测** — 运行；新增/修改/删除一名学生、新增一间寝室，`SELECT TOP 10 * FROM OperationLog ORDER BY op_id DESC;` 应能看到对应的 学生/寝室 记录。

- [ ] **Step 9: 提交**

```bash
git add src/DormManagement/Forms/StudentForm.cs src/DormManagement/Forms/RoomForm.cs
git commit -m "feat: 学生/寝室增删改记录操作日志"
```

---

## Task 6：住宿三窗体记录日志（HousingLog 保留）

**Files:** Modify `Forms/CheckInForm.cs`、`Forms/TransferForm.cs`、`Forms/CheckOutForm.cs`

- [ ] **Step 1: CheckInForm — 改 `CheckIn` 的写分配语句**

把 `CheckIn()` 里第一条 `DBHelper.Execute("INSERT INTO Allocation...")` 替换为（HousingLog 那条保持不变）：

```csharp
                var sno = grid.CurrentRow?.Cells["学号"].Value?.ToString() ?? "";
                var sname = grid.CurrentRow?.Cells["姓名"].Value?.ToString() ?? "";
                DBHelper.ExecuteLogged("INSERT INTO Allocation(student_id,bed_id) VALUES(@s,@b)",
                    new[] { new SqlParameter("@s", sid), new SqlParameter("@b", bid) },
                    "入住", "新增", $"入住 {sname}({sno})");
```

- [ ] **Step 2: TransferForm — 改 `Transfer` 的更新语句**

把 `Transfer()` 里 `DBHelper.Execute("UPDATE Allocation SET bed_id...")` 替换为：

```csharp
                var sno = grid.CurrentRow?.Cells["学号"].Value?.ToString() ?? "";
                var sname = grid.CurrentRow?.Cells["姓名"].Value?.ToString() ?? "";
                DBHelper.ExecuteLogged("UPDATE Allocation SET bed_id=@b WHERE student_id=@s",
                    new[] { new SqlParameter("@b", bid), new SqlParameter("@s", sid) },
                    "调宿", "修改", $"调宿 {sname}({sno}) → 床位{bid}");
```

- [ ] **Step 3: CheckOutForm — 改 `CheckOut` 的删除语句**

`CheckOut()` 已有 `var name = ...`。把 `DBHelper.Execute("DELETE FROM Allocation WHERE student_id=@s", ...)` 替换为：

```csharp
            DBHelper.ExecuteLogged("DELETE FROM Allocation WHERE student_id=@s",
                new[] { new SqlParameter("@s", sid) },
                "退宿", "删除", $"退宿 {name}");
```

> 三处的第二条 `INSERT INTO HousingLog(...)` 保持原样（住宿流水与操作日志并存）。`ExecuteLogged` 抛出的 2627 仍会被各窗体原有的 `catch when (ex.Number is 2627 or 2601)` 捕获。

- [ ] **Step 4: 构建** — `BUILD`，预期 0 错误。

- [ ] **Step 5: 手测** — 运行；入住一名学生、再退宿，`SELECT TOP 10 * FROM OperationLog ORDER BY op_id DESC;` 应看到 入住/退宿 记录；同时 `SELECT TOP 5 * FROM HousingLog ORDER BY log_id DESC;` 仍有对应流水（两者并存）。

- [ ] **Step 6: 提交**

```bash
git add src/DormManagement/Forms/CheckInForm.cs src/DormManagement/Forms/TransferForm.cs src/DormManagement/Forms/CheckOutForm.cs
git commit -m "feat: 入住/调宿/退宿记录操作日志（HousingLog 保留）"
```

---

## Task 7：备份恢复窗体加"操作时间线"

**Files:** Modify `Forms/BackupRestoreForm.cs`

- [ ] **Step 1: 加 using 与时间线 grid 字段**

文件顶部 `using` 区加一行：
```csharp
using DormManagement.DAL;
```
在 `txtLog` 字段定义之后，加 grid 字段：
```csharp
        readonly DataGridView gridLog = new()
        {
            Dock = DockStyle.Fill, ReadOnly = true, AllowUserToAddRows = false,
            SelectionMode = DataGridViewSelectionMode.FullRowSelect, MultiSelect = false,
            AutoGenerateColumns = true, AutoSizeColumnsMode = DataGridViewAutoSizeColumnsMode.Fill
        };
```

- [ ] **Step 2: 把 `txtLog` 从 Fill 改为 Bottom 固定高度**

在 `txtLog` 初始化器里把 `Dock = DockStyle.Fill` 改为 `Dock = DockStyle.Bottom, Height = 120`：
```csharp
        readonly TextBox txtLog = new()
        {
            Multiline = true, Dock = DockStyle.Bottom, Height = 120, ReadOnly = true,
            ScrollBars = ScrollBars.Vertical, Font = new Font("Consolas", 9), BackColor = Color.White
        };
```

- [ ] **Step 3: 构造函数里加按钮、重排布局、接事件**

把构造函数中"按钮定义 → 布局 → 事件接线"整段（从 `var btnFull` 到 `btnRestore.Click += ...` 之间，含 `Controls.Add`）替换为：

```csharp
            var btnFull = new Button { Text = "完整备份", Width = 90 };
            var btnLog = new Button { Text = "日志备份", Width = 90 };
            var btnRestore = new Button { Text = "恢复到所选时间点", Width = 140 };
            var btnRefreshLog = new Button { Text = "刷新日志", Width = 80 };
            var btnBefore = new Button { Text = "恢复到选中之前", Width = 120 };
            var btnAfter = new Button { Text = "恢复到选中之后", Width = 120 };

            var top = new FlowLayoutPanel { Dock = DockStyle.Top, Height = 120, Padding = new Padding(8) };
            top.Controls.AddRange(new Control[] {
                btnFull, btnLog,
                new Label{ Text="时间点", AutoSize=true, Padding=new Padding(14,10,0,0) }, dtp, btnRestore,
                btnRefreshLog, btnBefore, btnAfter });

            Controls.Add(gridLog);  // Fill（先加）
            Controls.Add(txtLog);   // Bottom
            Controls.Add(top);      // Top（后加）

            Load += (_, _) =>
            {
                Directory.CreateDirectory(BackupDir);
                Append($"备份目录：{BackupDir}");
                Append("流程：完整备份 → 改数据/误操作 → 日志备份 →（刷新日志）选中一条操作 → 恢复到之前/之后。");
                LoadOpLog();
            };

            btnFull.Click += (_, _) => FullBackup();
            btnLog.Click += (_, _) => LogBackup();
            btnRestore.Click += (_, _) => RunRestore(dtp.Value);
            btnRefreshLog.Click += (_, _) => LoadOpLog();
            btnBefore.Click += (_, _) => RestoreBySelectedRow(before: true);
            btnAfter.Click += (_, _) => RestoreBySelectedRow(before: false);
            gridLog.SelectionChanged += (_, _) =>
            {
                if (gridLog.CurrentRow?.Cells["时间"].Value is DateTime t
                    && t >= dtp.MinDate && t <= dtp.MaxDate) dtp.Value = t;
            };
```

- [ ] **Step 4: 把原 `Restore()` 方法改为 `RunRestore(DateTime t)`**

把现有 `void Restore() { ... }` 整个方法替换为下面两个（签名改为接收时间，并在成功/失败后刷新时间线）：

```csharp
        void RunRestore(DateTime t)
        {
            if (!File.Exists(FullPath)) { MessageBox.Show("未找到完整备份文件，请先执行完整备份"); return; }
            if (!File.Exists(LogPath)) { MessageBox.Show("未找到日志备份文件，请先执行日志备份"); return; }

            if (MessageBox.Show(
                    $"将把 DormDB 恢复到 {t:yyyy-MM-dd HH:mm:ss.fff}。\n" +
                    "期间会断开该库所有连接并覆盖当前数据，确认继续？",
                    "确认时间点恢复", MessageBoxButtons.YesNo, MessageBoxIcon.Warning) != DialogResult.Yes)
                return;
            try
            {
                ExecMaster(
                    @"ALTER DATABASE DormDB SET SINGLE_USER WITH ROLLBACK IMMEDIATE;
                      RESTORE DATABASE DormDB FROM DISK=@full WITH NORECOVERY, REPLACE;
                      RESTORE LOG DormDB FROM DISK=@log WITH STOPAT=@t, RECOVERY;
                      ALTER DATABASE DormDB SET MULTI_USER;",
                    new SqlParameter("@full", FullPath),
                    new SqlParameter("@log", LogPath),
                    new SqlParameter("@t", t));
                Append($"[时间点恢复] 成功，已恢复到 {t:yyyy-MM-dd HH:mm:ss.fff}");
                MessageBox.Show("恢复成功");
            }
            catch (SqlException ex)
            {
                Fail("时间点恢复", ex);
                BringOnline();
                MessageBox.Show("时间点恢复失败：" + ex.Message +
                    "\n\n已自动将数据库恢复到完整备份时刻并重新上线，可正常登录。");
            }
            LoadOpLog();   // 回滚后日志也回到该刻，刷新时间线
        }

        // 时间线选中一行 → 计算 STOPAT（+500ms 缓冲确保跨过该笔事务提交点）→ 恢复
        void RestoreBySelectedRow(bool before)
        {
            if (gridLog.CurrentRow?.Cells["编号"].Value is not int opId
                || gridLog.CurrentRow.Cells["时间"].Value is not DateTime opTime)
            { MessageBox.Show("请先在操作时间线中选择一条记录"); return; }

            DateTime stopAt;
            if (before)
            {
                var prev = DBHelper.Scalar("SELECT MAX(op_time) FROM OperationLog WHERE op_id < @id",
                    new SqlParameter("@id", opId));
                if (prev is null or DBNull)
                { MessageBox.Show("这已是最早的操作记录；如需更早请直接还原完整备份基线。"); return; }
                stopAt = Convert.ToDateTime(prev).AddMilliseconds(500);   // 含前一条，排除选中这条
            }
            else
            {
                stopAt = opTime.AddMilliseconds(500);   // 含选中这条
            }
            RunRestore(stopAt);
        }

        void LoadOpLog()
        {
            try
            {
                gridLog.DataSource = DBHelper.QueryTable(
                    @"SELECT op_id AS 编号, op_time AS 时间, category AS 类别, action AS 操作,
                             description AS 描述, operator AS 操作人
                      FROM OperationLog ORDER BY op_id DESC");
                if (gridLog.Columns.Contains("编号")) gridLog.Columns["编号"].Visible = false;
            }
            catch (SqlException ex) { Append("[操作时间线] 加载失败（OperationLog 是否已建？）：" + ex.Message); }
        }
```

> STOPAT 用 `op_time + 500ms`：日志与数据同事务提交，加 0.5 秒缓冲确保 STOPAT 落在该笔事务提交之后、下一笔之前（手工操作通常间隔数秒）。

- [ ] **Step 5: 构建** — `BUILD`，预期 0 错误。

- [ ] **Step 6: 手测（完整 PITR 走查）**
  1. 打开"备份与恢复" → 点 **完整备份**。
  2. 关窗口，去"学生管理"**新增**一名学生"赵六"。
  3. 回"备份与恢复" → 点 **刷新日志**，应看到"学生/新增/新增学生 赵六(...)"。
  4. 再去把"赵六"**删除**（模拟误删）。
  5. 回"备份与恢复" → **日志备份**。
  6. **刷新日志**，选中"删除学生 赵六"那条 → 点 **恢复到选中之前** → 确认。
  7. 重新登录，"学生管理"里**赵六还在**（误删被撤销）。

- [ ] **Step 7: 提交**

```bash
git add src/DormManagement/Forms/BackupRestoreForm.cs
git commit -m "feat: 备份恢复窗体加操作时间线，可选中操作回滚到其之前/之后"
```

---

## Task 8：更新用户使用手册

**Files:** Modify `docs/用户使用手册.md`

- [ ] **Step 1:** 在"6. 备份与恢复"小节，把"6.1 演示 PITR 的正确顺序"更新为基于操作时间线的新流程：完整备份 → 改数据/误操作 → 日志备份 →（刷新日志）选中目标操作 → 恢复到之前/之后；并说明时间线各列含义与"之前/之后"语义。

- [ ] **Step 2: 提交**

```bash
git add docs/用户使用手册.md
git commit -m "docs: 用户手册更新为基于操作时间线的时间点恢复流程"
```

---

## 自检对照（计划覆盖 spec）

| spec 要求 | 对应 Task |
|---|---|
| OperationLog 表（§3.1） | Task 1 |
| Session.CurrentOperator（§3.2） | Task 2 |
| ExecuteLogged 事务内写数据+日志（§3.2） | Task 3 |
| 7 类写操作记录日志（§3.2、§5） | Task 4、5、6 |
| 备份窗体操作时间线 + 之前/之后（§3.3） | Task 7 |
| 复用现有 STOPAT 恢复（§3.4） | Task 7（RunRestore 复用 ExecMaster/BringOnline） |
| "之前"最早一条提示（§4） | Task 7 RestoreBySelectedRow |
| HousingLog 保留并存（§3.1） | Task 6 |
| 更新使用手册（§5 文件表） | Task 8 |
| 不破坏现有功能、build 通过（§6） | 各 Task 的构建步骤 |
